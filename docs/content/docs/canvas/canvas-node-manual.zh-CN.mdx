---
title: 画布节点操作手册
description: 当前画布节点的主要用途与操作流程
---

# 画布节点操作手册

本文档记录画布节点的主要用途和操作流程，方便使用和后续开发维护。当前先介绍文本节点。

## 文本节点

文本节点用于保存提示词、草稿、说明文案和 AI 生成的文字结果。它既可以作为独立文本内容，也可以作为下游生成配置节点的输入。

### 编辑文本内容

- 双击文本节点内容区域，直接编辑节点里的文字。
- 选中文本节点后，点击顶部工具栏的“编辑文字”，进入文字编辑状态。
- 顶部工具栏的“缩小”和“放大”用于调整文本节点字号。

### 用下方对话框生成或修改文本

- 选中文本节点后，点击顶部工具栏的“编辑”，打开节点下方对话框。
- 文本节点下方对话框只用于生成或改写文本，不承担生图功能。
- 当文本节点为空时，输入框用于填写想生成的文本内容；点击发送后，结果会回填到当前文本节点。
- 当文本节点已有内容时，输入框用于填写想把本段文本修改成什么；点击发送后，会在右侧生成新的文本节点，并自动连接原节点和新节点。
- 输入内容可以手写，也可以从提示词库选择。
- 输入框支持 `@` 引用已连接的图片、文本、视频、音频资源；`@` 引用图片时会在输入框内直接显示真实缩略图，发送时按当前连接自动编号交给模型。因此不需要再记忆「图片1 / 文本1」编号，画布节点右上角也不再显示资源角标。
- 对话框里的模型下拉来自全局配置里已拉取的模型列表；选择结果只作用于当前节点，不会修改其它节点或全局默认模型。
- 如果下拉中没有模型，需要先打开配置弹窗拉取模型列表，并设置默认生图模型和默认文本模型。

### 用文本节点生成图片

- 先在文本节点中准备好要用于生图的文本内容。
- 点击文本节点顶部工具栏的“生图”按钮。
- 系统会在文本节点右侧自动创建一个生成配置节点，并连接文本节点到生成配置节点。
- 生成配置节点会读取上游文本内容作为生图提示词，并立即开始生成图片。
- 后续需要调整模型、分辨率（1k / 2k / 4k / 自动）、宽高比、数量时，可以在生成配置节点里修改后再次生成。

### 用图片节点继续生成图片

- 选中空图片节点后点击“生成”，会直接在当前节点里生成结果，不再额外保留一个空框。
- 当数量为 `1` 时，生成结果会直接回填到当前图片节点。
- 当数量大于 `1` 时，当前图片节点会作为主图节点，右侧继续生成对应数量的子图节点。
- 如果图片节点本身已经有内容，则会沿用已有图片作为参考图，再生成新的图片批次。
- 每个生成出来的图片节点都会在自身 `metadata` 下记录提示词、生成类型、模型、尺寸、质量、数量和参考图引用，方便后续重试。
- 如果重试时参考图已经丢失或无法恢复，系统会直接提示，避免误发空的图生图请求。
- Tripo 渠道同样可用于图片生成，模型列表是它自己的一套（`seedream`、`banana`、`chat_image` 三个系列）。各系列接受的尺寸参数不同，所选分辨率和宽高比会自动换算成该模型接受的形式，并就近贴到它允许的档位。Tripo 没有数量参数，要多张图会发多个任务、分别计费。

### 视频节点

- 工具栏可以新建视频节点，也可以拖入或上传本地视频文件。
- 视频节点使用原生播放器展示内容，可在节点内直接播放、暂停和拖动进度。
- 空视频节点下方对话框可输入提示词生成视频，结果会回填到当前节点。
- 从文本、图片或配置节点创建视频生成时，会在右侧生成新的视频节点并自动连接。
- 生成配置节点的视频模式会读取上游文本作为 prompt，读取上游图片作为参考图，读取上游视频作为参考视频，读取上游音频作为参考音频，并在输入预览里显示这些参考。未配置调用脚本时，OpenAI 和 Gemini 内置请求与插入模板使用相同字段；自定义视频调用脚本可通过 `videos`、`audios` 变量读取这些参考文件。
- 视频设置使用 `480p` / `720p` / `1080p` 与 `1:1`、`3:4`、`4:3`、`16:9`、`9:16`、`21:9` 或自动计算宽高，时长为 4–30 秒滑杆。也支持「首尾帧」和「全能参考」两种模式。首尾帧模式下 1 张图作为首帧、2 张图作为首尾帧发送专门字段；超过 2 张图时即使选了首尾帧也会按全能参考发送。内置请求和调用脚本都会使用解析后的 `frames` 或 `reference`。
- OpenAI 风格接口使用 `POST /v1/videos`、`GET /v1/videos/{id}` 和 `GET /v1/videos/{id}/content`，按模式发送 `first_frame` / `last_frame` 或 `image[]`，并可附带 `video[]` / `audio[]`。Gemini 格式渠道使用 `models/{model}:predictLongRunning` 并轮询 operation，按模式发送 `image` / `lastFrame` 或 `referenceImages`。
- 创建远端任务后会把任务 ID 写入视频节点；刷新页面后会自动继续查询任务状态，也可点击「获取任务状态」手动拉取，直到生成成功、任务失败或查询请求报错。

### 3D 节点

- 需要先在配置弹窗添加 Tripo 渠道（apiFormat 选「Tripo」），并开启「本地代理」：Tripo 的 `/v3` 接口不返回 CORS 头，浏览器无法直连。
- Tripo 分国内版（`openapi.tripo3d.com`）和海外版（`openapi.tripo3d.ai`）两个独立区域，apiFormat 选「Tripo」后渠道编辑器会出现区域切换；两个区域是独立账号，各渠道需要各自填写 API Key。
- 工具栏和连线创建菜单都可以新建 3D 节点；节点内使用可拖拽旋转、滚轮缩放的 3D 查看器展示 glb 模型，右上角的交互开关用来在「旋转模型」和「移动节点」之间切换。
- 空 3D 节点下方对话框输入提示词即走文生 3D；上游连接图片节点时会先把图片上传到 Tripo，再走图生 3D。
- 3D 设置包含贴图、PBR 材质、贴图质量（标准/精细/极致）和面数上限。面数上限留空为自动；各模型允许范围不同，超范围时直接显示 Tripo 返回的提示。
- Tripo 生成是异步任务：创建后任务 ID 会写入节点，刷新页面会自动继续查询，直到成功、任务失败或查询报错。文生 3D 实测约 3 分钟、消耗 10–30 credits，图生 3D 约 80 秒、20 credits。
- 生成成功后会立即把 glb 和预览图下载到浏览器本地存储再显示，因为 Tripo 的签名地址 24 小时后失效。右键节点可下载 `.glb`；也可「导入 Blender」（经 Tripo Bridge 的 `ws://127.0.0.1:60600` 发送已存文件）或「打开 Bambu Studio」（`bambustudio://open?file=` 指向 3MF）。多选或选中含 3D 的组可一起操作。导入 Blender 需要用 `http://localhost` 打开画布，HTTPS 页连不上本机 ws；3MF 不会发给 Blender。打开 Bambu Studio 需要先转成 3MF，Studio 自己去拉 HTTP 地址：转换时记下的 Tripo 地址仍有效就用它，否则由 Canvas Agent 临时托管本地文件。

- 3D 操作作用在已有模型的节点上：悬停节点打开「3D 操作」，包含重新贴图、自动绑骨、套用动画、减面、分割部件、网格补全和转换格式。各操作共用一个参数弹窗（字段按操作不同），除转换外都会在右侧生成新的 3D 节点并连线；转换写回原节点。
- 其中两个操作只能接在本画布的产物后面：「套用动画」需要在这里绑过骨的节点，「网格补全」需要在这里分割过的节点。Tripo 这两个接口只接受上游任务 ID、不接受上传文件，因此在其他节点上会置灰并说明原因。其余操作对任意 3D 节点都可用，原任务 ID 丢失时会重新上传本地模型。自动绑骨会先跑 Tripo 免费的可绑骨检测，模型不可绑骨时直接给出可读提示且不消耗 credits。
- 转换格式支持 FBX、GLTF、USDZ、OBJ、STL、3MF（不含 GLB，Tripo 明确拒绝——它本就是原生输出）。这些格式无法在 glb 查看器里显示，所以产物留在原节点上，「3D 操作」菜单会多出对应的下载项。转换结果存在本地，重开项目后仍可下载。
- 多视图：给 3D 生成连 2–4 个图片节点会走多视图生成 3D，而不是只用第一张。正面视图必填，所以第一个连上的图片默认作为正面，除非它已带有来自多视图生成的视图归属；超过四张会提示，而不是静默丢弃。图片节点的「多视图」工具（默认关闭，因为会消耗 credits）可把单图变成网格排列的四个视图节点；对其中一个再次执行会打开编辑器，只填部分视图就只改这几个。

## 组节点

组节点用来把多个节点收成一块，方便一起移动，也可以作为生成输入一次性引用组内资源。

- 框选或按住 `Shift / Ctrl / Cmd` 点选多个节点后，会显示包住全部选中节点的圆角虚线框，框上方出现「打组」。
- 点击「打组」或按 `Ctrl / Cmd` + `G`，会新建一个组节点包住这些内容，节点保持原位置。
- 选中组后，可用顶部工具条的「解散组」、右键菜单，或 `Ctrl / Cmd` + `Shift` + `G` 取消分组。
- 也可以先在工具栏新建空组，再把节点拖进去；拖出组外会自动取消归属。
- 把组连到生成节点后，组内有内容的图片、视频、音频、文本和可引用扩展节点都会作为输入。

## 推荐流程

1. 新建文本节点，写入图片创作想法。
2. 打开文本节点下方对话框，让 AI 优化或扩写这段提示词。
3. 改写结果会生成到新的文本节点，保留原始文本方便对比。
4. 确认文本节点内容后，点击顶部工具栏“生图”。
5. 在自动创建的生成配置节点中继续调整图片参数或重新生成。
