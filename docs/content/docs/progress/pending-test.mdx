---
title: Pending Tests
description: Implemented changes that still need manual verification
---

# Pending Tests

The current release needs manual verification in these areas:

- Tripo 3D generation: the channel editor should offer "Tripo" as an apiFormat, filling the base URL with `https://openapi.tripo3d.com`; the capability list should include "3D", and the model list should offer `v3.1-20260211`, `v3.0-20250812`, `v2.5-20250123`, `P1-20260311`, and `P2-20260801` directly (Tripo has no model-list endpoint, so no `/v1/models` request should be made). After saving the channel and reloading the page, its apiFormat must still be Tripo rather than OpenAI. Preferences should show a default 3D model.
- Tripo 3D generation flow: Tripo's `/v3` endpoints send no CORS headers, so the local proxy must be enabled; with it off, generation should report a readable error instead of failing silently. The configuration node's mode selector should include "3D", and both the text prompt panel and the 3D node prompt panel should show the 3D model picker and 3D settings. A prompt alone runs text-to-3D; an upstream image node uploads the image first and runs image-to-3D. While generating, the node shows a progress placeholder; on success it should show a model that rotates by dragging and zooms by scrolling (three.js loads from a CDN on demand, so a network connection is required), with the preview image shown first as a placeholder. Measured: text-to-3D about 3 minutes and 10–30 credits, image-to-3D about 80 seconds and 20 credits.
- Tripo 3D settings: the 3D settings panel should offer texture, PBR material, texture quality (standard/detailed/extreme), and a face limit. An empty face limit means auto; a value outside the model's range should surface Tripo's own message verbatim (ranges differ per model, e.g. 48–50000 for P2) rather than being blocked client-side. Changes are recorded on the node, and regenerating reuses that node's settings.
- Tripo 3D persistence and resume: reloading mid-generation should resume polling from the stored task id instead of turning the node into an error; only a terminal Tripo failure clears the task id, while a timeout or network failure keeps it retryable. After a successful generation, closing and reopening the canvas should read the model and preview back from local storage (Tripo's signed URLs expire within a day, so the remote URL must not be relied on), and this should still work with the local proxy switched off. Right-clicking a 3D node should download a `.glb`. Placing more than three 3D nodes and repeatedly deleting and recreating them should not blank the canvas or exhaust WebGL contexts.
- Tripo image generation: a Tripo channel's model list should also offer the image models `seedream_v5`, `seedream_v4`, `banana`, `banana_pro`, `banana2`, `chat_image_2`, `chat_image_2.5_flare`, `chat_image_2.5_sunburst`, `chat_image_1.5`, and `chat_image_1`, each classified as an image capability rather than 3D. A prompt alone runs text-to-image; connecting reference image nodes uploads each one and runs image-to-image. Tripo has no `n` parameter, so requesting several images issues several tasks and each is billed separately. Results should be fetched from the CDN and stored locally like other image generations — image outputs come from a CloudFront host that sends no CORS headers (unlike the 3D output CDN), so that download goes through the local proxy as well and should not raise a fetch error in the console. Image generation goes through the same `/v3` endpoints as 3D, so it needs the local proxy too; with the proxy off, both should fail immediately with a message telling you to enable it rather than only logging CORS errors to the console.
- Tripo image sizing per model: each family accepts different size arguments, so the request should adapt instead of failing. `seedream_v4` accepts only the 2K and 4K tiers and `seedream_v5` only 2K and 3K, so a 1K or 4K canvas setting should snap to the nearest accepted tier rather than returning a 1004. `banana` and `banana_pro` accept only a fixed pixel list (`1024x1024`, `832x1248`, and so on) and otherwise fall back to `aspect_ratio`. `banana2` takes a K-tier plus `aspect_ratio`. The `chat_image` models take an explicit WxH or the literal `auto`, never a K-tier, and only `chat_image_2`, `chat_image_2.5_flare`, and `chat_image_2.5_sunburst` accept `quality` (sending it to any other model is a hard 1004); `background` applies only to the two 2.5 models.
- Tripo regions: two channels should ship by default, one for the China region (`https://openapi.tripo3d.com`) and one for the global region (`https://openapi.tripo3d.ai`), each pre-populated with `v3.1-20260211` and `seedream_v5`. With apiFormat set to Tripo, the channel editor should show a region picker that rewrites the base URL, and it should be hidden for other formats. The two regions are separate accounts, so each channel keeps its own API key and a key from one region should not authenticate against the other; both regions need the local proxy.
- 3D node double-click to enlarge: double-clicking a 3D node that holds a model should open a large viewer (80vw wide, 72vh tall) titled with the node name, where dragging to orbit and scrolling to zoom work without first switching the node to interact mode. The double-click should work whether the node is in move or interact mode. An empty 3D node should not open anything; image nodes should still open the image preview, text nodes should still enter editing, and double-clicking empty canvas should still open the create menu. Opening and closing several 3D viewers repeatedly should not blank the canvas or exhaust WebGL contexts (the modal uses destroyOnHidden so the context is released on close).
- 3D viewer render modes: the large viewer should offer five modes in its top-left corner — Shaded, Clay, Clay + wire, Wireframe, Skeleton (the canvas node itself shows no mode buttons and keeps the plain shaded look). Clay should be untextured neutral grey so only the form reads; Clay + wire should show the topology without the lines z-fighting against the faces; Wireframe should sit on a dark ground with cyan lines; Skeleton should make the mesh translucent and draw the rig on top of it (visible through the surface). Switching modes must not reload the model or lose the camera angle, and a model with no bones should not show the Skeleton button at all. After cycling through every mode several times, returning to Shaded should restore the original textured materials intact.
- 3D nodes show a thumbnail by default: with several 3D nodes on the canvas, each should show a still of the model (with a "Click to load model" hint in its corner) and load the real model only when clicked. The point is performance: with twenty-odd 3D nodes, panning and zooming the canvas should be clearly smoother than before and the console should not warn about WebGL contexts being reclaimed. At most 4 nodes render live at once, so opening a fifth returns the least recently opened one to its still — that is by design, not lost state. A node should not be returned to a still while you are dragging its model.
- 3D node auto thumbnails: a 3D node with no preview image (most easily reproduced with locally split part nodes; Tripo-generated nodes come with their own preview) should briefly load once, capture a still, and show that from then on. Check that splitting out a dozen parts at once has them complete one after another rather than all sitting on the placeholder (only 4 render at any moment), and that the canvas ends up with no live contexts once they finish. The stills should survive a page reload (they live in local storage, not a remote URL), and should be swept away with the node when it is deleted.
- 3D viewer on-demand rendering: a still model should not redraw continuously. Verify that switching render mode (Shaded / Base color / Clay / Wireframe / Skeleton) updates immediately, that resizing the large viewer reframes the model, and that "Reset camera" returns to the opening framing — each of these depends on explicitly requesting a frame, and a missing one would look like "clicking does nothing". An animated model should play smoothly, and pausing should stop it without leaving the loop running.
- 3D viewer base color mode: the large viewer should show a "Base color" button next to "Shaded", and switching to it should draw the model with its base color (albedo) texture only — no shading, no highlights or shadows, colors flat and even; the difference from "Shaded" is that lighting no longer affects it at all (which is the point when checking flat anime-style textures). Check a few material kinds: a normal textured result should show the texture's own colors; a PBR result should show no metalness or roughness response in this mode; a result with an untextured solid-color material should show that color rather than turning black; parts whose texture has an alpha channel (hair, lashes) should stay cut out rather than going opaque. Every group of a multi-material FBX result (materials split into groups) should render correctly, not just the first one. Switching to base color and back to shaded should restore the textures and materials intact, and a rigged model should still animate normally in this mode.
- FBX loading: H-series (v3.x) quad output, and auto-rig or apply-animation results with fbx as the output format, are FBX files and should now display and orbit in both the canvas node and the large viewer just like glb, rather than spinning forever or reporting a load failure (only GLTFLoader existed before). The format is decided from the file's magic bytes (`glTF` for glb, `Kaydara FBX` for FBX) rather than the URL extension, since models are stored as blob URLs. A rigged FBX should show its skeleton and play its clips the same way. Conversion output in OBJ / STL / USDZ / 3MF is still download-only and is not previewed.
- Built-in four views: the image node's hover toolbar should offer a "Four views" button next to Tripo's "Multiview" (both visible by default). It should fire four edit requests against the configured image model and channel (not the 3D model), producing front/left/back/right images placed in a 2×2 grid to the right of the source and wired from it, each titled with its view. The four run in parallel, so check the partial-failure path: if one view fails, the message should say how many failed and the successful views should still be kept, not discarded — only an all-four failure should surface as an error. The resulting nodes carry their view assignment, so connecting them to a 3D node should run multiview-to-3D. Note that built-in views have no Tripo task id, so they should not open the multiview edit dialog (that is only for Tripo-produced sets); clicking "Multiview" on one should regenerate instead. Consistency between views is up to the image model and needs a real-model check.
- Multiview empty-upload fix: clicking "Multiview" on an image node (the Tripo one, not the built-in four-view tool) should now run through instead of failing with `code: 2003` "The image file is empty" — the node's blob URL was being treated as a data URL, so a zero-byte file was uploaded. Verify that path itself: a single image should produce four views. Try it both right after the image is generated and after reloading the page (the node's blob URL comes from a different place in each case). Clicking "Multiview" on an existing four-view set runs the edit path and uploads nothing, so it should be unaffected; the built-in four-view tool, image-to-3D, multiview-to-3D, and multi-reference image edits should all still work too (those already read to a data URL first).
- Four views concurrency and rate limits: the four views are requested **serially**, not in parallel — image providers commonly allow one concurrent generation, and firing four at once trips "You have exceeded the limit of generation" (Tripo `code: 2000`). So check that one click produces the four images one after another rather than failing as a batch. On an actual rate limit the message should read "Hit the generation limit; retrying this view in Ns" and that view should be retried once after the wait; N comes from the response's `Retry-After` header, falls back to 4 seconds without it, and is clamped to 2–30 seconds so an absurd header cannot stall the run. If the retry also fails, only that view counts as failed and the rest are kept. Cancelling during the wait should stop immediately rather than sitting out the backoff. Non-limit failures such as 401 or a content policy rejection (1004) should not trigger a retry and should just fail that view.
- Model part splitting: the 3D node's "3D ops" → "Segment parts" now has a "Split into part nodes" switch (on by default). With it on, a segmentation run should produce — alongside the whole segmented node — one 3D node per part in a column to its right (roughly face, hair, body, arms for a character), each titled with its part name, each wired from the segmented node, and each independently previewable, downloadable, and usable for further 3D ops. Parts are split from the returned glb in the browser (Tripo returns one combined glb, not separate files), so check that each part keeps its position within the whole rather than collapsing to the origin, and that several meshes of one part (hair_1, hair_2, Hair.003) merge into a single node instead of three. The v1.0 segmentation model does no semantic labeling and its meshes may carry no part names; that case should report "no recognizable part names" and keep only the whole node, rather than erroring or producing one node duplicating the whole — test with v2.0-20260430. With the switch off, behavior should be exactly as before. Part nodes have no Tripo task id of their own, so a follow-up op on one should upload its file rather than chaining off a task.
- Quad wireframe: a model generated with quad topology should show quad edges in Wireframe and Clay + wire, not an extra diagonal inside every quad. **This did not work on the first acceptance pass** (triangles were still drawn): shared edges were matched by vertex index, but exporters split vertices at UV and normal seams, so the two triangles of one quad hold different indices and not a single shared edge matches. Vertices are now welded by position (5 decimal places) before the topology test, so this needs another look on a real quad model: the wireframe should show quad loops. Diagonals are identified as edges shared by exactly two triangles that are the longest edge of both — on an 8×8 square grid all 64 diagonals are recovered both before and after welding (i.e. with exporter-style split vertices), and welding a 25000-triangle mesh (the face-limit ceiling) takes about 30ms, so switching modes should stay responsive. Two known edge cases are worth checking: a mesh of near-equilateral triangles ties on "longest" and would be misread, so the recovery is abandoned and the plain triangle wireframe is used instead (a regular octahedron misreads 100% of its edges, hence the guard) — triangle-topology models should therefore look exactly as before; a rigged model also falls back to triangles, because quad edges live in a separate line geometry that cannot skin (better a triangle wireframe than one frozen in bind pose).
- 3D viewer skeleton and animation: a rigged model (an auto-rig result, or an applied-animation result) should show its skeleton in the large viewer with the bone count in the bottom-left corner. A model carrying animation clips should show play/pause in the bottom-right, plus a clip dropdown when there is more than one clip, and the clip count bottom-left. While an animation plays, switching to Wireframe or Clay + wire should make the wireframe move with the bones rather than freezing in bind pose. Pausing and then switching mode or clip should not silently resume playback. The camera reset button should return to the framing you got on open. A static model with neither bones nor clips should read "Static mesh" bottom-left and show no play button.
- Tripo readable auth errors: with a wrong API key, or a China-region key saved on the global-region channel (the two regions are separate accounts — verified: the same key works on `.com` and returns "Invalid API key" on `.ai`), image generation, 3D generation, reference-image upload, and every 3D operation should report an authentication failure that explains keys are not shared across regions, instead of only "Request failed with status code 401" that forces you into the network tab. With the proxy toggle on but the command not running, the error should say the local proxy is unreachable. Task-level failures (content policy, timeout) and cancellation messages should be unchanged.
- Tripo 3D topology: the 3D settings panel should show a "Topology" row (Triangles / Quads) when P2, v3.0, or v3.1 is selected, and hide it for P1 and v2.5 — P1 rejects the field outright (verified: "quad is only supported by model P2-20260801"), so it is not sent for unsupported models. Choosing Quads should show a note that on the H series (v3.x) the output becomes FBX and cannot be previewed on the canvas, only downloaded (worth confirming with a real generation), and that P2's face limit drops to 48–25,000 — exceeding it should surface Tripo's own message. The setting is stored on the node and reused when regenerating. Check what actually gets recorded, too: generating with P1 or v2.5 while the global setting still says Quads should record triangles on the node (the field is not sent for those models anyway), otherwise the viewer tries to recover quad loops from a triangle-topology result.
- Tripo image-to-3D auth: with a Tripo API key configured on the channel (image generation already working), image-to-3D, multiview-to-3D, and image-to-multiview should no longer report "Configure the API key first" — the reference-image upload step was not resolving the model's channel and read the empty top-level apiKey. Worth checking with several channels configured (e.g. OpenAI first, Tripo later in the list).
- 3D prompt box hidden once images are connected: connecting an image to a 3D node should remove the prompt input entirely and put a one-line explanation in its place — one image says image-to-3D, two to four says multiview (noting that images from a multiview run keep their own angle while the rest fill the remaining front/left/back/right angles in connection order), more than four says only the first four are used — and each variant should say no prompt is needed. The expand-editor button and the prompt library should disappear with it (there is nothing to expand), while the model picker and 3D settings stay and the generate button stays clickable. Disconnecting every image should bring the input back with any previously typed prompt intact. Connecting an image while the expanded editor is open should close that modal rather than leaving an input whose text is discarded. The text, image, video, and audio modes should be unaffected.
- Retrying a prompt-free task: make a generation that needs no prompt fail (easiest to reproduce with image-to-3D or multiview-to-3D: connect images, leave the prompt empty, then break the network or use a wrong key), then click retry on the node — it should actually re-issue the request rather than warning that the prompt is missing and refusing. A multi-reference image edit (image node with images connected and an empty prompt) should likewise be retryable. Check the guard was not loosened too far: a text or audio node with an empty prompt should still warn on retry (those APIs genuinely require one), and so should an image or 3D node with neither a prompt nor connected reference images.
- Where 3D retry goes: retrying a failed 3D node should re-run 3D generation and write the model back onto that same node — not create a new node, and definitely not run an image request (retry had no branch for 3D nodes and fell through to the image path, writing an image into the 3D node's content). Test both text-to-3D and image-to-3D failures. The video node's "Query task status" button shares the retry callback, so confirm it still only queries status and does not kick off a regeneration.
- Tripo optional 3D prompt: with images connected to a 3D node, the generate button should work with an empty prompt (image-to-3D and multiview-to-3D need no prompt, and Tripo does not accept one), and the placeholder should say a prompt is optional. With no images connected the prompt is still required (text-to-3D) and the button stays disabled. Text, image, video, and audio modes are unchanged — an empty prompt still cannot generate. Also, the 3D prompt box previously had no placeholder text at all and rendered the raw translation key; it should now show real text in both languages.
- Tripo multiview to 3D: connecting 2–4 image nodes to a 3D generation should run multiview-to-model instead of using only the first image. The front view is required, so images that carry their own angle claim it first and the rest fill the still-unclaimed angles in connection order (with no carried angles anywhere, the first image is the front). Connecting more than 4 images should warn that only the first 4 were used, rather than silently dropping the rest. A single image should still run image-to-3D and a prompt alone text-to-3D.
- No duplicate multiview angles: angles are assigned across the whole set, so no combination of connections should produce the same angle twice. Check these collisions in particular: (1) connect one "left" image from a multiview set plus one plain image — the result should be left + front, not two lefts (the old per-image rule assigned the second image left as well by index, which both duplicated an angle and left front unclaimed, so Tripo rejected the request with "front view is required"); (2) connect two "front" images from two different multiview sets — the result should be front + left, not two fronts (the server accepts this one silently and returns a mangled mesh, making it the hardest case to notice); (3) one "right" image plus three plain images — the result should be right + front + left + back, all four angles present and distinct. Also confirm the normal cases still work: a full four-view set should keep each image on its own angle even when connected out of order, and an all-plain set should still map to front/left/back/right in connection order. A set that cannot supply a front view at all (e.g. only "back" + "right") should surface Tripo's front-view-required error rather than having the front silently reassigned.
- Multiview auto-grouping: after either the built-in "Four views" or Tripo's "Multiview" finishes, the four images should be wrapped in a group node (titled "Four views", showing its member count), with each image still wired from the source node. Check that dragging the group moves all four images rather than just the frame; that connecting the group to a 3D node feeds all four views at once (no need to wire each image), with each image's angle still taken from its own view assignment regardless of order within the group; and that wiring both the group and its members to the same 3D node does not count eight images. The group is a dashed frame painted beneath the images, so it should neither cover them nor block clicks. Right-clicking the group should ungroup it (leaving the four images and their connections intact), and deleting the group should follow existing behaviour for its members. A run where only one view succeeded should produce no group (matching the two-member minimum for manual grouping). Re-running a multiview edit should produce a new set with its own group rather than joining the old one.
- Generation history: the left panel's "History" tab should list every generation in this canvas, newest first. Run each kind once: image (including a multi-image batch, which should produce a single record), video, audio, text-to-3D, image-to-3D, multiview-to-3D, image-to-four-views, edit multiview, the 3D operations (texture / rig / retarget / decimate / segment / complete / convert), local mask edit, angle variants, and a retry after a failure. Each row should show the kind icon, a status dot (running / success / error), the time, and the model; everything that goes through Tripo should carry a task ID, while synchronous endpoints such as image and audio have none and their copy button should be disabled. Text generation is not recorded.
- Generation history across interruptions: refresh the page while a video or 3D generation is running — that record should stay "running" with its task ID already stored, then flip to success once the resumed poll finishes (with no duplicate record added), or to error with the message if the resume fails. Pressing "Stop" mid-run should close the record as an error labeled "Request canceled" rather than leaving it stuck on "running".
- Generation history search, filtering, and cleanup: searching by prompt, model name, or task ID should match; the kind dropdown should narrow to that kind only; the locate button should center the matching node, and should be disabled with a "The node was deleted" hint once that node is gone; deleting one record and "Clear" should affect only the current canvas and survive a refresh (records live in localforage, not memory); deleting a whole canvas should drop that canvas's records and leave other canvases' records untouched.
- Generation history stores metadata only (known limitation): delete a generated node without saving it as an asset, and after a storage cleanup pass the result file is gone — the record remains but only helps you look the task ID or prompt up and generate again, it cannot restore the file. The note at the bottom of the panel should always say so. History is local-only for now and is not part of WebDAV sync.
- Parameter rows (ComfyUI style): selecting a node should show a "Parameters" toggle at the bottom of its panel, which expands into the full parameter rows for that mode. Check all five: image (quality / size / count / transparent background), video (duration / resolution / ratio / audio / watermark / mode), audio (voice / format / speed / instructions), 3D (texture / PBR / texture quality / topology / face limit), and text (reasoning effort / count). Changing any row should write to the node immediately, survive a refresh, and be the value the request actually uses. The expanded/collapsed state is a session UI preference and is not written into the canvas document (a refresh returns to expanded).
- Parameter rows must not change node geometry: expanding or collapsing the parameter area must not alter the node's size, position, image aspect ratio, or scaling; the panel is still the 600px overlay below the node.
- Config node as a parameter node: create a config node and drag from its right-hand output dot (previously hidden) to a generation node. Its own "Generate" button should disappear, replaced by "Parameter node, used by N downstream node(s)", and its panel should switch from the prompt composer to parameter rows. The downstream node's rows should become read-only and name the source node, with a locate and a disconnect button; disconnecting should make them editable again.
- Parameter node precedence: global config < the node's own metadata < the parameter node. Change texture quality on the parameter node and the downstream 3D node should send that value even if it stored a different one itself; disconnecting should fall back to the node's own value. One parameter node wired to several nodes should apply to all of them (the count should match). Retries and 3D follow-up operations (retexture / rig / decimate and the rest) should inherit it too.
- A parameter node is not material: after wiring it up, the downstream node's reference bar should not show the config node as a thumbnail and its reference count should be unaffected. A config node with nothing wired out should still behave as the fan-in hub it is today (several sources into it, it into a generation node).
- Existing canvases must not mistake a config node for a parameter node: every generation from a config node also connects it to the result it produced, and those connections carry no parameter tag, so config nodes in existing canvases should still show "Generate" and the prompt composer. Verify with an old canvas where a config node has generated before — its behaviour should be identical to before the upgrade; only a connection dragged by hand from the config node's right-hand dot turns it into a parameter node. A parameter connection should also never count as a generation input, and should not interfere with finding the original config node when retrying a failure (the retry should still carry the original prompt and references).
- View labels on connections: connect two or more images to a 3D node and each connection should show a front / left / back / right label at its midpoint, matching exactly what the request sends (a single image runs image-to-3D and should show no label; beyond four images only the first four are labelled). Labels should follow the connection while zooming the canvas and dragging nodes.
- Reassigning a view: clicking a label should open the view picker. Choosing a view already held by another image of the same 3D node should swap the two rather than leaving both on the same view (the set of views stays complete, front stays claimed, and Tripo's `front view is required` cannot be tripped). The new assignment should survive a refresh and be reused by the next generation.
- Labels on a grouped connection: group several images and connect the group to a 3D node — that single connection should list each image's view joined by "·", and clicking the label should list the images individually so each can be reassigned on its own.
- 3D viewer panning: in both the node preview and the double-click window, right-drag (two-finger drag on a trackpad) should move the model within the frame, while left-drag still orbits and the wheel still zooms. The model should track the pointer, and the drag direction should stay consistent after orbiting round to the side or back. Right-clicking inside the viewer should not open the node's context menu (the menu on empty canvas and on other nodes is unaffected). "Reset camera" should clear the pan along with the orbit, returning to the default X-axis view. On a 3D node that is not interactive, dragging should still move the node itself rather than being swallowed by the viewer.
- Face limit slider: the 3D settings' face limit should be a 48-25000 slider with a step of 100, defaulting to 5000, with a number box beside it for exact entry (out-of-range values clamp to the bounds, an unusable value falls back to 5000). With the "Auto" switch on, the slider should be hidden and no face_limit sent; with it off, the slider value is used. Both the switch and the value must survive a refresh — in particular check that Auto stays on after a reload (it is stored as an explicit auto marker, because a blank would be overwritten by the default on every load).
- Face limit migration (behaviour change): a global setting left blank (auto) before the upgrade migrates to 5000, so existing canvases generating 3D will now be capped at 5000 and differ from before; anyone who wants Tripo to decide has to switch Auto back on. A face limit stored on the node itself, or supplied by a parameter node, should still take precedence over the global setting.
- Model name on multiview-to-3D: connecting 2-4 images and running multiview-to-3D should no longer fail with `invalid model 'DQdLKtaxCCl5eFsShrZSi::P2-20260801'` (1004). The model picker stores "channelId::model" so two channels can offer the same model name, and the multiview path was passing that composite value straight into the request body. The stripping now lives in runModelOp, so all seven 3D operations (retexture, rig, retarget, segment, complete, decimate, multiview-to-3D) are worth a pass to confirm their model names still go out correctly — especially with several channels configured where two offer a 3D model of the same name.
- Tripo multiview images: an image node's toolbar has a "Multiview" tool (off by default — it spends credits, unlike the local image tools; enable it via the toolbar's "more" settings). On a plain image it generates four views, which land as four image nodes in a 2x2 grid to the right, each connected to the source and labelled front/left/back/right. Running the tool on one of those generated view nodes should instead open the edit dialog, where filling in only some views edits just those. Editing requires the original multiview task, so it only works on nodes produced by a multiview run in this canvas.
- Tripo 3D operations menu: hovering a 3D node that holds a model shows a "3D ops" menu with retexture, auto-rig, apply animation, decimate, segment parts, complete mesh, and convert format. Each operation opens one shared parameter dialog whose fields differ per operation. Every operation except conversion should create a new 3D node to the right of the source, connected to it, and store its own task id so the chain can continue.
- Tripo operation chaining limits: "Apply animation" must be greyed out with an explanation on any node that was not rigged in this canvas, and "Complete mesh" likewise on any node that was not segmented here — Tripo only accepts the upstream task id for these two and rejects an uploaded file. On nodes that do qualify, both should run. Every other operation should work on any 3D node, including one restored after reopening the project (it re-uploads the stored model to get a token when the task id is gone).
- Tripo auto-rig pre-check: rigging first runs the free rig-check. For a model Tripo reports as non-riggable, generation should stop with a readable message naming the detected type and consume no credits. Note the rig model enums differ from the generation models, and only some rig types have preset animations, so the animation list is empty for those.
- Tripo format conversion: conversion writes back to the SAME node rather than creating one (the formats cannot be previewed in the glb viewer), and the "3D ops" menu then shows a "Download FBX"-style entry for the produced format. GLB is not offered because Tripo rejects it. After a conversion, closing and reopening the project should keep the download working — the converted blob must survive the unused-media sweep. Refreshing mid-conversion should resume the task rather than losing it, since conversions are billed.
- Local proxy: Settings should show a dedicated Local proxy tab between Providers and Preferences, with a switch that is off by default. Turning it on should reveal the `npx @basketikun/canvas-proxy@latest` command (with `--port` appended when a non-default port is configured), a copy action, and an address field defaulting to `http://127.0.0.1:23210` that normalizes a missing scheme and trailing slashes on blur. Test connection should succeed only while the command is running. With the proxy on, fetching model lists, generating images, videos, text (including streamed output), and audio, running custom call scripts, polling video tasks, downloading generated images and videos, and WebDAV connection tests plus sync should all route through the proxy and keep working; turning it off should restore direct requests. The proxy itself should forward request methods, headers, query strings, and bodies unchanged, stream SSE responses without buffering, and keep `data:` and `blob:` URLs untouched.
- Image and video size settings: Image settings should split resolution (`1k` / `2k` / `4k` / auto) from aspect ratio (`1:1`, `2:3`, `3:2`, `4:3`, `3:4`, `16:9`, `9:16`, `21:9`, `9:21`, auto) and fill W/H from that pair. Video settings should keep `480p` / `720p` / `1080p` (plus a custom `p` value) and `1:1` / `3:4` / `4:3` / `16:9` / `9:16` / `21:9` / auto, then fill W/H from that pair. Duration should be a 4–30 second slider with the current value on the right; auto ratio should disable W/H.
- Built-in video requests: without a custom call script, OpenAI-format channels should send `mode`, `first_frame`/`last_frame` or `image[]`, `generate_audio`, `watermark`, and reference videos/audio like the insertable template, and must not use `input_reference[]`. Gemini-format channels should use `predictLongRunning` with `image`/`lastFrame` or `referenceImages`, and must not report that video is unsupported. Refreshing the page should continue polling a Gemini task.
- Video generation mode: Video settings should offer First & last frame and General reference. One image is sent as the first frame, two images as first and last frames, and more than two images as general references even if first-and-last-frame mode is selected. Built-in requests and request scripts should use `frames` or `reference`.
- Gemini image generation: Gemini-format channels and the Gemini image call-script template should send `generationConfig.responseModalities: ["TEXT", "IMAGE"]` with `imageConfig.aspectRatio` / `imageConfig.imageSize`, and must not send `responseFormat`.
- Video request scripts: opening Call Script for a Video model should open a full-screen stepped editor. The left panel should follow Read the rules → Let another AI write it → Available variables, and list `videos` (File[]) and `audios` (File[]). Copy script brief should include the capability, variables, return requirements, and writing rules. Inserted templates should be async functions whose parameter lists unpack `params` (image templates must include `quality`, `size`, `count`, and `background`). The OpenAI video template should use FormData with `mode`, duration, size, resolution, audio, watermark, and reference images/videos/audios; `mode=frames` should send `first_frame` / `last_frame`, and `mode=reference` should send `image[]`, then poll and download `/content`. After pasting a script from another AI and saving, generating from the canvas with connected reference videos or audio should pass those files into the script.
- URL credential import: opening the app with a new valid `baseUrl` and `apiKey` should append a provider without changing existing providers; importing the same Base URL again, including trailing-slash or `/v1` variants, should update only that provider and remain idempotent; an API key without a Base URL or an invalid Base URL should leave all settings unchanged.
- Documentation navigation and page contents: at phone width, opening the documentation sidebar should show the current section and its description at the top. Expanding it should allow switching among Overview, Canvas Guide, Development, Progress, Business, and Support; the selected section and page list should update together while the desktop top tabs remain unchanged. At widths of 960px and above, a table of contents with hierarchical track lines should appear to the right of the article, support heading navigation, and highlight the current heading while scrolling; narrower layouts should keep the top collapsible contents control.
- Canvas Agent generation-flow references: when a generation prompt contains only `@[node:...]` references already passed through `referenceNodeIds`, the existing text, image, video, audio, or group nodes should connect directly to the generated config node without creating a duplicate prompt text node; prompts containing ordinary prose should continue creating their own prompt text node.
- Video frame capture: right-clicking a video node with content should show Capture first frame, Capture last frame, and Capture current frame. Each action should create and select a proportional image node to the right, connect the video to the image, use the player's visible position for the current frame, and leave the original playback position unchanged.
- Text nodes: the Generate button should no longer appear in the upper-right corner of the node body, and text should use the full content area. The image-generation action in the hover toolbar should continue to create a generation configuration.
- Multi-text generation: requesting more than one result should create one text-result node and one source connection. Its upper-right count should expand all successful alternatives like a multi-image node; setting an alternative as primary should immediately replace the body while retaining the previous primary, and edits to any primary text should remain when switching away and back.
- Group references: group nodes should use a transparent, lightweight border and title treatment and expose a source handle on the right. Connecting a group to a generation node should include every populated image, video, audio, text, and reference-capable extension node inside it. Regular prompt mentions should list the group's resources, while the generation-config composer should allow selecting the whole group with one `@`; resources referenced both individually and through the group must be sent only once.
- Group selected nodes: after selecting two or more nodes, a rounded dashed bound should wrap the whole selection, with Group above it. It wraps those nodes in a group without moving them, then selects the new group. Selecting a group should allow Ungroup from the hover toolbar, context menu, or `Ctrl/Cmd+Shift+G`. Nodes already in the same group should show Ungroup instead of grouping again. Dragging into a group, group references, and the left-panel tree should keep working.
- Node reference bar: regular prompt panels, expanded editors, and generation-config inputs should show their current input nodes above the editor. Images and videos should use thumbnails with hover previews, while text, audio, and extension nodes show their matching type. A group connection should expand into every valid content node inside the group, and removing any expanded item should disconnect the whole group. The plus button should enter canvas selection for adding multiple valid references. Connected nodes, the target itself, children already referenced through a group, and unsupported nodes must not be selectable again; ESC or the top hint should return to the input, and new connections should immediately update the `@` list.
- Multi-image transparency: when a multi-image node containing transparent PNG or WebP assets is expanded, transparent areas in both the primary and alternatives should reveal the canvas; switching primary images must not introduce a white or panel-colored background.
- Canvas navigation: the canvas should open in Move mode by default, and the bottom toolbar should switch between Select and Move modes and reflect the active mode with its icon; Select should draw a clearly spaced dashed range whose dash length, gap, and stroke width remain visually constant at every zoom level, while Move should drag elements directly and pan only when dragging the blank background. Clicking blank canvas space in either mode should deselect the current node and close its top toolbar and lower prompt panel. Holding Control or Space should temporarily invert the active tool without changing the toolbar mode: Select becomes Move, and Move becomes range selection. After clicking any canvas-page button, pressing Space should only invert the tool and must not trigger that button again. Middle-button panning from any position and additive node selection with Shift/Cmd should continue to work, while Space remains available inside text inputs and editable content.
- English routes without a locale prefix should load directly without a redirect loop; switching between English and Simplified Chinese should keep navigation, settings, titles, descriptions, Ant Design components, and persisted preferences in the selected language.
- Global Dropdown, Menu, Select, Cascader, and TreeSelect popup backgrounds, hover states, and selected states in both light and dark themes.
- Canvas node resize stability: importing an image should open its prompt panel, resizing it immediately from any corner should temporarily hide the panel, remain smooth without flashing or triggering a maximum update depth error, and restore the panel after release. Prompt scrolling, generated-prompt restoration, edit-prompt persistence after closing an existing image or text node panel, expanded prompt editing with live synchronization and `@` references, image editing, drag-and-drop references, and generation configuration should continue to work.
- Text output count: both a text node's lower prompt panel and a generation configuration node in Text mode should expose an independent generation count from 1 to 15, default to one, and create that many text nodes. Changing the text count must not alter the image count, and switching a generation configuration node back to Image should continue to use the configured image count.
- Node editing entry points: the image toolbar should default to icons without labels and no longer include Edit; the text toolbar should no longer include Edit or Edit text. Clicking an image, text, or video node without dragging should open its lower prompt panel directly, while image toolbar labels can still be enabled explicitly from the toolbar settings.
- Canvas side panel: clicking an element row should smoothly center and select its node without zooming above 100%; image elements with content should still open the large preview without triggering canvas focus.
- 画布左侧树形列表：组内节点应缩进显示在所属组下方，并带有清晰的层级连接线；组箭头可以收起和展开子节点，搜索或类型筛选时仍应保留匹配节点所属的组。
- Multi-image canvas generation: starting N images should immediately create one image node with N expandable slots and one incoming connection; each empty slot should independently show generating or failure while requests are running, every slot with image content should always display that image, and the first successful image should appear as the primary image immediately. The collapsed state should use a compact stack with at most three closely spaced backplates and a clearly legible count control; only clicking the count control should expand or collapse the slots upward and to the right, while double-clicking the image should open its large preview. Every completed secondary image should keep a clearly legible primary-image action visible in both themes. Expanded groups should remain expanded when clicking elsewhere, dragging, resizing, or previewing, and multiple groups may remain expanded together.
- Multi-image primary selection: choosing another primary image should preserve the node center and current maximum edge while adapting to the new image ratio; free-resize nodes should keep their exact dimensions, and the viewport zoom indicator must not change.
- Failed multi-image slots: every failed slot, including the current primary slot, should expose retry and delete actions. Retry should update only that slot without replacing an existing successful primary image; delete should remove only that failed slot and update the displayed count.
- Multi-image resize: resizing the root node while child images are expanded should keep the group expanded and resize normally without triggering nested React updates or an application error.
- Multi-image alternatives: expanded images should use up to four columns so larger batches stay compact; double-clicking any completed child image should preview that exact image without changing the primary image. Every completed image, including the primary image, should provide an independent download action, while alternatives should also keep clearly legible create-copy and set-primary actions in both themes. Create-copy should add an independent image node to the right of the group, select it, and leave the original image group unchanged.
- Canvas Agent startup, MCP status, model and permission controls, approvals, version reporting, diagnostics, streaming responses, history consistency, multi-tab isolation, and local Skill management.
- Canvas Agent credential redaction: Codex and Claude CLI stderr containing Bearer tokens, `sk-` keys, explicit API key/token/authorization assignments, ANSI formatting, or credentials split across output chunks should reach neither debug logs nor browser diagnostic events in plaintext; ordinary stderr text should remain intact.
- Image API model parameters: `gpt-image-*` generation and edit requests should omit `response_format`, while `dall-e-*` requests should continue sending `response_format: "b64_json"`.
- Image URL persistence: canvas and image-workbench generation should download each remote API result only once, validate it as a decodable image, and persist it in IndexedDB; `200` responses containing non-image data should fail, stopping canvas generation during download should not save or apply the result, CORS-blocked but displayable images should keep their original URL without an empty storage key, and plugin calls should still receive Data URLs whenever the remote image is readable.
- Multi-reference image editing: OpenAI-format edits with more than one reference image should submit files as repeated `image[]` form fields; a single reference should continue using `image`. Newly created OpenAI image scripts should use the same field names. Gemini-format channels and already saved custom scripts are unchanged.
- Agent header tabs in English and Chinese at different panel widths; labels should collapse to icons and counts before either edge is clipped.
- Agent message layout, Markdown, code blocks, attachments, token statistics, progress timelines, command groups, and scroll-follow behavior.
- Agent inline references: type `/` to select an enabled Skill or `@` to select a canvas asset at the caret; tokens should remain aligned with text, support keyboard navigation and hover previews, and preserve their positions after sending or refreshing.
- Agent message metadata: image attachments, Skills, canvas references, and local previews should survive page refreshes and Agent restarts through exact `clientMessageId`, `threadId`, and `turnId` ownership; protocol upgrades must not reset the independent storage format, unknown or damaged storage must refuse writes without overwriting files, no records may be silently pruned by count or size, and deleting a thread should remove its metadata and preview assets without relying on browser IndexedDB or text matching.
- Prompt Center layout, source synchronization, search, asset insertion, prompt detail dialogs, and compact split-layout prompt pickers in the image and video workbenches.
- Workbench history image cleanup: after generating content in the image or video workbench, deleting an asset, canvas image node, canvas content, or canvas project must not remove thumbnails, results, or references from generation history; deleting a generation record should still remove its dedicated local files.
- Prompt text labels: when a regular node or generation configuration is connected to several text nodes, every upstream text block appended to the prompt should start with its label such as `【Text 1】` or `【Text 2】`, numbered in connection order and matching both the `@` references in the prompt and the reference bar. A single text input should still be labeled, and image, video, and audio numbering must stay unchanged.
- Local mask editing: the dialog footer should offer Reset, Export to canvas, and Generate now. Both actions require a painted area and an edit request; Export to canvas should only write the nodes and connections while prefilling the composed prompt in the result node's prompt panel for manual generation, and Generate now should start the request right after writing the nodes. After painting an area and entering an edit request, confirming should add a mask annotation node below the source image showing the original picture with a translucent blue overlay on the painted area that matches the color and opacity used while painting in the dialog, create the result node to the right, and connect both the source image and the mask annotation to it. The request should be sent as two ordinary reference images with a prompt that names them by index, without the `mask` API parameter, and it should also work for Gemini-format channels; only the marked area should change, and the result must not keep any blue overlay.
- Settings import/export and WebDAV-related configuration.
- WebDAV canvas deletion: deleting a previously synced canvas and syncing again should not restore it from WebDAV; canvases created on another device and not deleted should still appear after sync. Refreshing before the next sync should still keep the deletion. Asset and generation-history deletions are unchanged.
- Local storage settings: the Settings page should provide a Local storage tab that reads only the `infinite-canvas` database and shows IndexedDB usage, total site usage, browser quota, quota percentage, and per-object-store record counts and content size; refreshing after local data changes should update the figures without blocking the rest of the page.
- Asset image download: opening an image that still previews in My Assets and clicking Download image should save the file. Titles with Chinese characters, spaces, or brackets such as `【】` should remain the downloaded filename and must not need renaming. Images saved from the canvas in the same session should download as well.
- Canvas video task resume: starting an OpenAI-style video generation should store the remote task ID on the video node. After a page refresh, a still-pending video should keep polling automatically; if polling stopped, **Get task status** should resume with that task ID until the video is ready, the task fails, or the status request errors. Task failure should then allow a normal retry that creates a new generation.

The Chinese version contains the complete item-by-item acceptance checklist.

## 图片分辨率尺寸表

选择分辨率和宽高比后，W/H 按下表填写，单位为像素。预设尺寸不受「16 倍数对齐」开关影响；该开关只控制手动输入尺寸时的对齐。最终生成尺寸以所选模型的实际输出为准。

| 宽高比 | 1K | 2K | 4K |
| --- | ---: | ---: | ---: |
| **1:1** | **1024 × 1024** | **2048 × 2048** | **2880 × 2880** |
| **2:3** | 1024 × 1536 | 1360 × 2048 | 2336 × 3520 |
| **3:2** | 1536 × 1024 | 2048 × 1360 | 3520 × 2336 |
| **4:3** | 1024 × 768 | 2048 × 1536 | 3312 × 2480 |
| **3:4** | 768 × 1024 | 1536 × 2048 | 2480 × 3312 |
| **16:9** | 1536 × 864 | 2048 × 1152 | **3840 × 2160** |
| **9:16** | 864 × 1536 | 1152 × 2048 | **2160 × 3840** |
| **21:9** | 2016 × 864 | 2688 × 1152 | **3840 × 1648** |
| **9:21** | 864 × 2016 | 1152 × 2688 | **1648 × 3840** |

待验收：

- 在画布和图像工作台逐项切换 3 档分辨率与 9 种比例，W/H 应与表格一致，重新打开设置后仍选中正确档位；重点检查 1K 的 2:3、16:9、21:9 及对应竖图，以及 4K 的 1:1。
- 所有 4K 预设提交时均不应触发本地总像素或最长边限制；生成和编辑请求、调用脚本应收到所选像素尺寸。
- 分辨率为自动时保留比例选择，比例为自动时禁用 W/H；手动输入 W/H 后，对齐开关仍应按原有方式工作。
- Gemini 在质量为自动时，应将表中尺寸识别为对应的 1K / 2K / 4K；显式选择质量时维持原有优先级。视频尺寸设置应保持原有行为。
