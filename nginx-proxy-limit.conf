# Shared with location /proxy/ in default.conf. 8 r/s is well above one person's generation
# and model-list traffic; burst 20 covers a short spike, then nginx answers 503.
limit_req_zone $binary_remote_addr zone=proxy:10m rate=8r/s;
